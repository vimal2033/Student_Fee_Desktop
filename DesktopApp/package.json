{
  "name": "fee-managment-desktop",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
 "build": "cd ../Student_Fee && npm run build && cd ../DesktopApp && (if exist builder rmdir /s /q builder) && (if exist dist rmdir /s /q dist) && mkdir builder && cmd /c xcopy /E /I /Y \"..\\Student_Fee\\dist\" \".\\builder\" && electron-builder",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1"
  },
  "devDependencies": {
    "electron": "^35.1.2",
    "electron-builder": "^26.0.12"
  }
}
